services:
  - type: web
    name: expense-tracker-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: Mongodb_Url
        sync: false
      - key: FRONTEND_URL
        sync: false
